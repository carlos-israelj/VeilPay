{
  "name": "veilpay-circuits",
  "version": "0.1.0",
  "description": "Circom circuits for VeilPay",
  "scripts": {
    "build": "bash ./scripts/build.sh",
    "test": "mocha test/*.test.js --timeout 100000"
  },
  "dependencies": {
    "circomlib": "^2.0.5",
    "snarkjs": "^0.7.0"
  },
  "devDependencies": {
    "circom_tester": "^0.0.19",
    "mocha": "^10.2.0",
    "chai": "^4.3.7"
  }
}
